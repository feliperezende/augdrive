#!/usr/bin/env sh

APP_BASE_NAME=`basename "$0"`
APP_HOME=`pwd`

exec java -Djava.awt.headless=true -Dorg.gradle.appname=$APP_BASE_NAME -classpath "$APP_HOME/gradle/wrapper/gradle-wrapper.jar" org.gradle.wrapper.GradleWrapperMain "$@"
